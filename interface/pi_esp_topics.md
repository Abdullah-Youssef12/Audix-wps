# Pi and ESP Topic Contract

This file defines the intended ROS and micro-ROS topic boundary between the Raspberry Pi and ESP32.

## Pi to ESP32

### Required
- `/cmd_vel` as `geometry_msgs/msg/Twist`
  Purpose: chassis-level motion command generated by `arena_roamer.py`

- `/robot_enable` as `std_msgs/msg/Bool`
  Purpose: enable or disable robot motion from the Pi side

### Optional future extension
- `/robot_stop` as `std_msgs/msg/Bool`
  Purpose: explicit stop channel if separate from enable semantics is desired later

## ESP32 to Pi

### Required for current high-level stack
- `/imu` as `sensor_msgs/msg/Imu`
  Purpose: IMU source for EKF

- `/odom` as `nav_msgs/msg/Odometry`
  Purpose: raw wheel odometry source for EKF

- `/ir_front_digital` as `std_msgs/msg/Bool`
- `/ir_front_left_digital` as `std_msgs/msg/Bool`
- `/ir_front_right_digital` as `std_msgs/msg/Bool`
- `/ir_left_digital` as `std_msgs/msg/Bool`
- `/ir_right_digital` as `std_msgs/msg/Bool`
- `/ir_back_digital` as `std_msgs/msg/Bool`
  Purpose: digital obstacle sensor states consumed by `ir_digital_bridge.py`

### Optional for hardware supervision
- `/limit_switch` as `std_msgs/msg/Bool`
- `/wheel_speeds` as a message to be defined later
- `/motor_status` as a message to be defined later

## Adapter Rules

To preserve `arena_roamer.py`, the system should keep these ROS-side topics stable:
- `/odometry/filtered`
- `/ir_front/scan`
- `/ir_front_left/scan`
- `/ir_front_right/scan`
- `/ir_left/scan`
- `/ir_right/scan`
- `/ir_back/scan`
- `/cmd_vel`
- `/robot_enable`

That means:
- `ir_digital_bridge.py` remains the translation layer from digital IR to `LaserScan`
- EKF remains the translation layer from raw odom plus IMU to filtered odometry
- low-level firmware should publish standard message types wherever possible

## Contract Stability

During early hardware bring-up:
- prefer remapping and bridge adaptation over changing `arena_roamer.py`
- keep message semantics simple and stable
- make the fake hardware test harness match this contract exactly